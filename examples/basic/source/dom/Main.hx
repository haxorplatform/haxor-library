package dom;

//Import classes so they can be found on 'index.html'
import dom.ButtonTimer;
import dom.InputTest;
import dom.TouchTest;
import dom.WebTest;
import dom.ButtonSound;
import dom.ButtonTween;

/**
 * Entry point where the examples are linked.
 * Select the example in the HTML
 * @author Eduardo Pons - eduardo@thelaborat.org
 */
class Main
{

	/**
	 * Entry point.
	 */
	static function main():Void
	{
		trace("Main> Starting DOM Examples");
		
		//===============================
		//Uncomment the demos on the HTML
		//===============================
	}
	
	
}